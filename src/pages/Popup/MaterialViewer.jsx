import React, { useState } from 'react';
import { Button } from '@material-ui/core'

const MaterialViewer = ({ material, setView }) => {
  console.log(material)

  return (
    <h1>matsku viwer</h1>
  );
};

export default MaterialViewer;
